# Fullstack Docker App

## Concept

This project is a simple fullstack application with a React frontend and a Node.js (Express) backend, containerized using Docker and managed with Docker Compose.

```text
frontend + backend → docker compose → multi-container app